# PostgreSQL tuning for ~4 GB RAM VPS (svoygarage.ru).
# Deployed to conf.d via update.sh ensure_postgresql_tuning().

shared_buffers = 512MB
effective_cache_size = 1536MB
work_mem = 8MB
maintenance_work_mem = 128MB
max_connections = 100
random_page_cost = 1.1
effective_io_concurrency = 200
